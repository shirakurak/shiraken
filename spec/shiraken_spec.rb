# frozen_string_literal: true

RSpec.describe Shiraken do
  it "has a version number" do
    expect(Shiraken::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
